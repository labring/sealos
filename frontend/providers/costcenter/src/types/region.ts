export type Region = {
  accountSvc: string;
  name: {
    zh: string;
    en: string;
  };
  domain: string;
  uid: string;
};

export type RegionClient = {
  name: {
    zh: string;
    en: string;
  };
  uid: string;
};
