# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "applaunchpad"

[setup]
script = ""

[[actions]]
name = "运行"
icon = "run"
command = '''
bash -lc '
set -euo pipefail

PROJECT_DIR="/home/sealos/labring/sealos/frontend/providers/applaunchpad"
KUBECONFIG_209="/home/sealos/.kube/209"
DEV_USER="5jbcgjlg"

cleanup() {
  for pid in ${port_forward_pids:-}; do
    kill "$pid" 2>/dev/null || true
  done
}
trap cleanup EXIT INT TERM

cd "$PROJECT_DIR"

node <<'"'"'NODE'"'"'
const { execFileSync } = require("node:child_process");
const fs = require("node:fs");
const yaml = require("js-yaml");

const kubeconfig = "/home/sealos/.kube/209";
const projectDir = "/home/sealos/labring/sealos/frontend/providers/applaunchpad";
const devUser = "5jbcgjlg";

function kubectl(args) {
  return execFileSync("kubectl", [...args, "--kubeconfig", kubeconfig], {
    encoding: "utf8"
  });
}

const userKubeconfig = kubectl([
  "get",
  "user",
  devUser,
  "-o",
  "jsonpath={.status.kubeConfig}"
]);
fs.writeFileSync(
  `${projectDir}/.env.local`,
  `NEXT_PUBLIC_MOCK_USER=${JSON.stringify(userKubeconfig)}\n`,
  { mode: 0o600 }
);

const configMap = JSON.parse(
  kubectl([
    "-n",
    "applaunchpad-frontend",
    "get",
    "cm",
    "applaunchpad-frontend-config",
    "-o",
    "json"
  ])
);
const config = yaml.load(configMap.data["config.yaml"]);
config.launchpad.components.monitor.url = "http://127.0.0.1:8428";
config.launchpad.components.log.url = "http://127.0.0.1:8429";
config.launchpad.components.billing.url = "http://127.0.0.1:2333";
fs.writeFileSync(`${projectDir}/data/config.yaml.local`, yaml.dump(config, { lineWidth: 120 }), {
  mode: 0o600
});
NODE

port_forward_pids=""
start_port_forward() {
  local namespace="$1"
  local service="$2"
  local local_port="$3"
  local remote_port="$4"

  kubectl --kubeconfig "$KUBECONFIG_209" -n "$namespace" port-forward --address 127.0.0.1 \
    "svc/$service" "${local_port}:${remote_port}" &
  port_forward_pids="$port_forward_pids $!"
}

wait_for_port() {
  local port="$1"
  for _ in $(seq 1 30); do
    if timeout 1 bash -c ":</dev/tcp/127.0.0.1/${port}" >/dev/null 2>&1; then
      return 0
    fi
    sleep 1
  done
  echo "Timed out waiting for localhost:${port}" >&2
  return 1
}

start_port_forward sealos launchpad-monitor 8428 8428
start_port_forward sealos service-vlogs 8429 8428
start_port_forward account-system account-service 2333 2333

wait_for_port 8428
wait_for_port 8429
wait_for_port 2333

[ -d node_modules ] || pnpm install
export NODE_TLS_REJECT_UNAUTHORIZED=0
exec pnpm run dev
'
'''
