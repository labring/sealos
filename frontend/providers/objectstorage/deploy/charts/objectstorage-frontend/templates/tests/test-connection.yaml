apiVersion: v1
kind: Pod
metadata:
  name: {{ include "objectstorage-frontend.fullname" . }}-test-connection
  namespace: {{ .Release.Namespace }}
  labels:
    helm.sh/chart: {{ include "objectstorage-frontend.chart" . }}
    app.kubernetes.io/name: {{ include "objectstorage-frontend.name" . }}-test
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    helm.sh/hook: test
    helm.sh/hook-delete-policy: before-hook-creation
spec:
  restartPolicy: Never
  containers:
    - name: wget
      image: {{ .Values.tests.image | quote }}
      imagePullPolicy: {{ .Values.tests.imagePullPolicy }}
      command:
        - wget
      args:
        - --spider
        - --timeout=10
        - {{ printf "http://%s:%v%s" (include "objectstorage-frontend.fullname" .) .Values.service.port .Values.tests.path | quote }}
