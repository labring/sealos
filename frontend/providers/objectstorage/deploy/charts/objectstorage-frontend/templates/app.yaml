apiVersion: app.sealos.io/v1
kind: App
metadata:
  name: objectstorage
  namespace: app-system
spec:
  data:
    desc: object storage
    url: {{ include "objectstorage-frontend.url" . | quote }}
  displayType: normal
  i18n:
    zh:
      name: 对象存储
    zh-Hans:
      name: 对象存储
  icon: {{ printf "%s/logo.svg" (include "objectstorage-frontend.url" .) | quote }}
  menuData:
  name: Object Storage
  type: iframe
