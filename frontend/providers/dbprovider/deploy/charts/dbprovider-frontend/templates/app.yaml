apiVersion: app.sealos.io/v1
kind: App
metadata:
  name: dbprovider
  namespace: app-system
spec:
  data:
    desc: Database
    url: {{ include "dbprovider-frontend.url" . | quote }}
  icon: {{ printf "%s/logo.svg" (include "dbprovider-frontend.url" .) | quote }}
  i18n:
    zh:
      name: 数据库
    zh-Hans:
      name: 数据库
  menuData:
  name: Database
  type: iframe
  displayType: normal
