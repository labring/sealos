FROM scratch

COPY etc etc
COPY scripts scripts
COPY registry registry

ENV cloudDomain="cloud.example.com"
ENV tlsCertFilePath="/etc/tls/cert.pem"
ENV tlsKeyFilePath="/etc/tls/key.pem"

CMD ["bash scripts/init.sh"]
