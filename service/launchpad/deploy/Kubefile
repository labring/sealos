FROM scratch
COPY charts charts
COPY registry registry
COPY manifests manifests
COPY launchpad-monitor-entrypoint.sh launchpad-monitor-entrypoint.sh

ENV RELEASE_NAMESPACE=sealos
ENV RELEASE_NAME=launchpad-monitor

CMD ["bash", "launchpad-monitor-entrypoint.sh"]
