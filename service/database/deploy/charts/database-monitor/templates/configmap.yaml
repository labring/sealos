apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-config
  labels:
    {{- include "database-monitor.labels" . | nindent 4 }}
    app: {{ .Release.Name }}
data:
  config.yml: |
    server:
      addr: ":{{ .Values.service.port }}"
