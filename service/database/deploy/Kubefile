FROM scratch
COPY charts charts
COPY registry registry
COPY manifests manifests
COPY database-monitor-entrypoint.sh database-monitor-entrypoint.sh

ENV RELEASE_NAMESPACE=sealos
ENV RELEASE_NAME=database-monitor

CMD ["bash", "database-monitor-entrypoint.sh"]
