apiVersion: v1
kind: Service
metadata:
  name: {{ include "costcenter.fullname" . }}
  labels:
    {{- include "costcenter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "costcenter.selectorLabels" . | nindent 4 }}
