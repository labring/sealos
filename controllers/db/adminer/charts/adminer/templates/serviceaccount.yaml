apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "adminer.fullname" . }}-adminer-controller-manager
  labels:
  {{- include "adminer.labels" . | nindent 4 }}
  annotations:
    {{- toYaml .Values.adminerControllerManager.serviceAccount.annotations | nindent 4 }}