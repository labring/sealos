---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: devboxschedules.devbox.sealos.io
spec:
  group: devbox.sealos.io
  names:
    kind: DevBoxSchedule
    listKind: DevBoxScheduleList
    plural: devboxschedules
    singular: devboxschedule
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DevBoxSchedule is the Schema for the devboxschedules API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DevBoxScheduleSpec defines the desired state of DevBoxSchedule.
            properties:
              devBoxName:
                type: string
              scheduleTime:
                description: 'Format: RFC3339 (e.g., "2006-01-02T15:04:05Z")'
                format: date-time
                type: string
              scheduleType:
                default: Stopped
                description: ScheduleType defines the type of shutdown to perform
                enum:
                - Stopped
                - Shutdown
                type: string
              state:
                default: Pending
                description: State represents the current state of the scheduled shutdown
                enum:
                - Pending
                - Completed
                - Unknown
                - NotFound
                - NotMatch
                type: string
            required:
            - devBoxName
            - scheduleTime
            - scheduleType
            type: object
          status:
            description: DevBoxScheduleStatus defines the observed state of DevBoxSchedule.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
