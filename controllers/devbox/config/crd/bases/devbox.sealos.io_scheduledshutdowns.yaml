---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: scheduledshutdowns.devbox.sealos.io
spec:
  group: devbox.sealos.io
  names:
    kind: ScheduledShutdown
    listKind: ScheduledShutdownList
    plural: scheduledshutdowns
    singular: scheduledshutdown
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.devBoxName
      name: DevBox
      type: string
    - jsonPath: .spec.shutdownTime
      name: ShutdownTime
      type: string
    - jsonPath: .spec.shutdownType
      name: ShutdownType
      type: string
    - jsonPath: .status.state
      name: State
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ScheduledShutdown is the Schema for the scheduledshutdowns API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ScheduledShutdownSpec defines the desired state of ScheduledShutdown.
            properties:
              devBoxName:
                type: string
              shutdownTime:
                description: 'Format: RFC3339 (e.g., "2006-01-02T15:04:05Z")'
                format: date-time
                type: string
              shutdownType:
                default: Stop
                description: ShutdownType defines the type of shutdown to perform
                enum:
                - Stopped
                type: string
            required:
            - devBoxName
            - shutdownTime
            - shutdownType
            type: object
          status:
            description: ScheduledShutdownStatus defines the observed state of ScheduledShutdown.
            properties:
              state:
                default: Unknown
                description: State represents the current state of the scheduled shutdown
                enum:
                - Pending
                - Completed
                - Unknown
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
