# permissions for end users to view appactions.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: clusterrole
    app.kubernetes.io/instance: appaction-viewer-role
    app.kubernetes.io/component: rbac
    app.kubernetes.io/created-by: app
    app.kubernetes.io/part-of: app
    app.kubernetes.io/managed-by: kustomize
  name: appaction-viewer-role
rules:
- apiGroups:
  - app.sealos.io
  resources:
  - appactions
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - app.sealos.io
  resources:
  - appactions/status
  verbs:
  - get
