name: Build Frontend Providers Image

on:
  workflow_dispatch:
    inputs:
      push_mage:
        description: 'Push images'
        required: false
        type: boolean
  push:
    branches: [ "main" ]
    paths:
      - "frontend/providers/**"
      - ".github/workflows/frontend-providers.yml"
      - "!**/*.md"
      - "!**/*.yaml"
  pull_request:
    branches: [ "*" ]
    paths:
      - "frontend/providers/**"
      - ".github/workflows/frontend-providers.yml"
      - "!**/*.md"
      - "!**/*.yaml"

env:
  # Common versions
  GO_VERSION: "1.20"
  DEFAULT_OWNER: "labring"

jobs:
  resolve-modules:
    runs-on: ubuntu-20.04
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Resolve Modules
        id: set-matrix
        run: bash ./scripts/resolve-modules.sh ./frontend/providers

  image-build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        module: [ applaunchpad, bytebase, imagehub, terminal ]
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v4
        with:
          images: |
            ghcr.io/${{ github.repository_owner }}/sealos-${{ matrix.module }}-frontend
          tags: |
            type=raw,value=latest,enable=${{ github.ref == format('refs/heads/{0}', 'master') }}
            type=raw,value=dev,enable=true
            type=sha,enable=true,prefix=,suffix=,format=short

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to Github Container Hub
        if: ${{ (github.event_name == 'push') || (inputs.push_mage == true) }}
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GH_PAT }}

      - name: Build And Push
        uses: docker/build-push-action@v4
        with:
          context: ./frontend/providers/${{ matrix.module }}
          file: ./frontend/providers/${{ matrix.module }}/Dockerfile
          # Push if it's a push event or if push_mage is true
          push: ${{ (github.event_name == 'push') || (inputs.push_mage == true) }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

  cluster-image-build:
    if: ${{ (github.event_name == 'push') || (inputs.push_mage == true) }}
    needs:
      - image-build
    runs-on: ubuntu-latest
    strategy:
      matrix:
        module: [ applaunchpad, bytebase, imagehub, terminal ]
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Expose git commit data
        uses: rlespinasse/git-commit-data-action@v1

      - name: Prepare
        id: prepare
        run: |
          TAG=${GIT_COMMIT_SHORT_SHA}
          echo tag_name=${TAG} >> $GITHUB_OUTPUT
          echo old_docker_repo=ghcr.io/labring/sealos-${{ matrix.module }}-frontend >> $GITHUB_OUTPUT 
          echo old_docker_image=ghcr.io/labring/sealos-${{ matrix.module }}-frontend:dev >> $GITHUB_OUTPUT
          echo new_docker_repo=ghcr.io/${{ github.repository_owner }}/sealos-${{ matrix.module }}-frontend >> $GITHUB_OUTPUT
          echo new_docker_image=ghcr.io/${{ github.repository_owner }}/sealos-${{ matrix.module }}-frontend:${TAG} >> $GITHUB_OUTPUT
          echo cluster_repo=ghcr.io/${{ github.repository_owner }}/sealos-cloud-${{ matrix.module }}-frontend >> $GITHUB_OUTPUT
          echo cluster_image=ghcr.io/${{ github.repository_owner }}/sealos-cloud-${{ matrix.module }}-frontend:${TAG} >> $GITHUB_OUTPUT

      - name: Install sealos
        uses: labring/sealos-action@v0.0.7
        with:
          sealosVersion: "4.1.7"

      - name: Mutate image tag in deploy files
        working-directory: frontend/providers/${{ matrix.module }}/deploy
        run: |
          sudo sed -i "s;${{ steps.prepare.outputs.old_docker_image }};${{ steps.prepare.outputs.new_docker_image }};" manifests/*

      - name: Sealos login to ghcr.io
        run: |
          sudo sealos login -u ${{ github.repository_owner }} -p ${{ secrets.GH_PAT }} --debug ghcr.io

      - name: Build ${{ matrix.module }}-frontend cluster image
        working-directory: frontend/providers/${{ matrix.module }}/deploy
        run: |
          sudo sealos build -t ${{ steps.prepare.outputs.cluster_image }}-amd64 --platform linux/amd64 -f Kubefile
          # delete old registry cache
          sudo rm -rf registry
          sudo sealos build -t ${{ steps.prepare.outputs.cluster_image }}-arm64 --platform linux/arm64 -f Kubefile

      - name: Manifest Cluster Images
        run: |
          sudo sealos images
          bash docker/patch/manifest-cluster-images.sh "sealos-cloud-${{ matrix.module }}-frontend"
        env:
          OWNER: ${{ github.repository_owner }}

      - name: Sync Images
        uses: peter-evans/create-or-update-comment@v1
        if: ${{ github.repository_owner == env.DEFAULT_OWNER }}
        with:
          issue-number: 251
          repository: labring-actions/cluster-image
          body: |
            /imagesync ghcr.io/${{ github.repository_owner }}/sealos-cloud-${{ matrix.module }}-frontend:${{ steps.prepare.outputs.tag_name }}
          token: "${{ secrets.GH_PAT }}"
